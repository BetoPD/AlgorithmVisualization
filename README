# Project Name

## Description

This project contains the main.py file, which is the main entry point for the application.

## Installation

1. Clone the repository.
2. Install the required dependencies by running the following command:

   ```shell
   pip install -r requirements.txt
